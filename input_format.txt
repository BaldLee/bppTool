2                      // size of V
X, Y                  // elements in V
2                     // size of Delta
[X, a, (Y)]          // rule 1
[Y, b, (X, X)]     // rule 2
X                     // the initial state
EG(AF(X + Y >= 2))            // EG logic formula
EG(E(a, (X + Y >= 2)))            // example of E<a> formula
EG(A(b, (X + Y >= 2)))            // example of A<b> formula
100                     // bound k



